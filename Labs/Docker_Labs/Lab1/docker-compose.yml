services:
  trainer:
    build:
      context: .
      dockerfile: dockerfile
    image: lab1:v1
    container_name: lab1-trainer
    environment:
      N_ESTIMATORS: 100
      TEST_SIZE: 0.2
    volumes:
      - ./src:/app
    working_dir: /app
    command: ["python", "main.py"]
